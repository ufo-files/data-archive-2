# NARA PDFs are immutable public records. Binary PDF bytes can match credential
# formats even though the files do not contain usable project credentials.
paths-ignore:
  - "National-Archives-UAP-Bulk/pdfs/**"
